import { SystemConfig } from 'configs'

export default (): SystemConfig => {
  return {
    ThemeMode: 'LIGHT',
    LayoutMode: 'SIDER_TOP_MODE',
    SettingDrawer: false,
    SiderCollapse: false
  }
}
